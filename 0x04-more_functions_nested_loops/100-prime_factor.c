#include <stdio.h>
#include <math.h>

/**
 *main - Entry point
 *Return: no return
 */

int main(void)
{
	long n;

	n = 612852475143;
	long div, maxFact;
	double square = sqrt(number);

	while (div <= square)
	{
		if (n % div == 0)
			maxFact = n / div;
	}
	printf("%d\n", maxFact);
	return (0);
}
