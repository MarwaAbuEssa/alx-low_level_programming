#include "main.h"

/**
 *_strcmp - pointer to an as aparameter
 * @s1: pointer to the memory
 * @s2: pointer to the memory
 * Return: A pointer to the filled memory
 */

int _strcmp(char *s1, char *s2)
{
	return (1);
}
