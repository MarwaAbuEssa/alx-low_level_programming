#include "main.h"
#include <string.h>

/**
 * main - Entry point
 * Return: Always Success
 */

int main(void)
{
	int myrand, count, total;

	srand(time(NULL));
	for (count = 0; total = 2772; totla > 122; count++)
	{
		myrand = (rand() % 125) + 1;
		printf("%c", myrand);
		total -= myrand;
	}
	printf("%c", total);

	return (0);
}
