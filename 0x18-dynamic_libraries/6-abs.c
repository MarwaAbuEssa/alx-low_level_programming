#include <stdio.h>
#include "main.h"

/**
 * _abs - print absolute value of int
 * @n: The character to be checked.
 * Return: +n if n > 0 else -n if n< 0
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
